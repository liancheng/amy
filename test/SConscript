env = Environment(CFLAGS='-ggdb')

env.Program(target='test',
            source=Glob('*.cpp'),
            LIBS=['gtest', 'gflags', 'glog', 'boost_filesystem', 'boost_system'])

# vim:ft=python ts=4 sw=4 et
